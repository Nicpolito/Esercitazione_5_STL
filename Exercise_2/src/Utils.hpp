#ifndef UTILS_HPP
#define UTILS_HPP

#include <vector>
#include <string>

// Definizione di funzioni di utilità per leggere file CSV
std::vector<std::vector<std::string>> readCSV(const std::string& filename);

#endif
